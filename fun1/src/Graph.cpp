//
// Created by gignt on 19.02.2024.
//

#include "Graph.h"

Graph::Graph(map<string,vector<pair<string,double>>> g){
    this ->graph = g;
    this ->numVertices = g.size();
}

Graph::Graph(Graph g, int distance){
    map<string,vector<pair<string,double>>> g;

    for (int i = 0; i < ; ++i) {

    }
}
